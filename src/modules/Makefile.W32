TOPDIR = ..\..

SUBDIRS = database audit authentication authorization filter ddlcompiler echoProcessor

!INCLUDE $(TOPDIR)\makefiles\nmake\sub.mk

local_all:

local_clean:

local_distclean:

local_test:
