Example showing the implementation of trees in the databse:

1) Create test database:

   sqlite3 lgtree.db < sqlite_schema.sql
   sqlite3 lgtree.db < sqlite_testdata.sql

2) Start server:

   wolframed -f -c wolframe.conf

