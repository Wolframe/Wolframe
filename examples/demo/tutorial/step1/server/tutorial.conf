; we install a verbose default logger to the shell
logging	{
	stderr	{
		level DEBUG
	}
}

; one connection, one thread is enough
server	{
	maxConnections 1
	threads 1
	listen	{
		address		*
		port		7661
		maxConnections	1
	}
}
