Directory for DTDs / Schemas / Relax-NG definitions and for some of the other XML things that we need. 

Note that this might not be (read probably will not be) the final place for those things.

configFile.rng: Relax-NG check for the configuration file
usage: xmllint --relaxng configFile.rng <config file>
