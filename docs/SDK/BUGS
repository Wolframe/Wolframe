1.1.2:
.. toplevel tag 'name' of the input. 
1.1.3:
declares the result tag for each result

=> tag/attribute in things which work with tables, can be
   confusing, element is a good name (is also used most
   of the times)

---

1.2.2.
The key is the name of the element and the following value is the assembles the attributes of this element.

=> ?

---

mixing DDL, transaction chapters (AppServer) and SDK parts
(like C++ forms and normalization functions) is not good
=> needs some splitting

---

1.3 Programming Lua

description of 'as' is maybe no longer true?

---

Glossary

The creator creates a command handler to execute a command.
