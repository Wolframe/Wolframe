<sect1>
<title>Implement Functions in .NET</title>
	<sect2>
		<title>Introduction</title>
		<para>You can write functions for the logic tier of <code>Wolframe</code> with languages based
		on <code>.NET</code> (<link>http://www.microsoft.com/net</link>) like for example <code>C#</code> and <code>VB.NET</code>.  
		Because <code>.NET</code> based libraries can only be called by <code>Wolframe</code> only as a 
		compiled and not as an interpreted language, you have to build a module out of your function 
		implementation.
		</para>
		<para>
		The implementation of <code>.NET</code> calls is not yet available. The implementation exists as
		prove of concept in a test program. But <code>Wolframe</code> will provide <code>.NET</code> functions 
		soon.
		</para>
	</sect2>
	<para></para>
</sect1>

