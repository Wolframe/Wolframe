TOPDIR = ../../..

SUBDIRS = employee_assignment_convert database functions cmdbind filter prnt

-include $(TOPDIR)/makefiles/gmake/sub.mk

local_all:

local_clean:

local_distclean:

local_test:

local_install:

local_uninstall:
